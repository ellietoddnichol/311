/**
 * The estimate experience is composed inside `ProjectWorkspace` on the `estimate` tab
 * (`EstimateToolbar`, `RoomList`, `EstimateGrid`, line drawer). This module exists so the
 * workflow page name stays discoverable for future route-level splits.
 */
export {};
